import React, { ReactNode } from "react";
import theme from "./theme";

export default function ThemeProvider({ children }: { children: ReactNode }) {
  // TODO: Implement theme context/provider logic if using styled-components or MUI ThemeProvider

  // Placeholder for theme context/provider
  return <>{children}</>;
}
