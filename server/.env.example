# Server Configuration
PORT=3001
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=djforever2_dev
JWT_SECRET=your_jwt_secret

# Frontend URL (for QR login redirects and email links)
FRONTEND_URL=http://localhost:3002

# Google Analytics 4
VITE_GA4_MEASUREMENT_ID=your-ga4-measurement-id

# SMTP Configuration for Email Reminders
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ⚠️ PRODUCTION EMAIL SAFETY GUARD ⚠️
# By default, emails are ONLY sent to whitelisted test addresses (see emailService.ts)
# All other emails are logged to console only, preventing accidental sends to real guests
# 
# To enable sending to ALL guests in PRODUCTION:
# 1. Verify guest list accuracy in userData.json
# 2. Set ENABLE_PRODUCTION_EMAILS=true
# 3. Ensure NODE_ENV=production
# 
# WARNING: Only enable this in production deployment after thorough testing!
ENABLE_PRODUCTION_EMAILS=false

# Production MongoDB Atlas URI (uncomment for production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DB_NAME=djforever2
# NODE_ENV=production
